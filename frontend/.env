# URLs relativas: em produção o nginx faz o roteamento.
# Não coloque IP aqui — quando o IP mudar basta rodar ./scripts/update-ip.sh
# sem precisar rebuildar o frontend.
VITE_API_URL=
VITE_WS_URL=/ws

# Para forçar SockJS (true) ou WebSocket nativo (false). Deixe vazio para auto.
# VITE_WS_SOCKJS=